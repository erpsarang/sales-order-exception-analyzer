{
  "name": "sales-order-exception-analyzer",
  "version": "0.0.0",
  "private": "true",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "npm run build && node --test --import tsx test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
